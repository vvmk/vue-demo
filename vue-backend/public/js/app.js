new Vue({
    el: '#root',

    mounted() {
        // Make an ajax request to our server. (/skills endpoint)
        // options: fetch() (need a polyfill)
        // $.ajax(), $.getJson() ...nah
        // axios <-- popular with vue people
    },
});

